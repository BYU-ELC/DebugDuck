// Author: Ben Broce (BYU ECEn ELC)

// Main project sketch file for Debug Duck
// - include all project libraries here for linking
// - basic setup() and loop() to call state machine functions

// libraries
#include <Arduino.h>

// modules
#include "pinDefs.h"

void setup() {
  // initialize hardware
  //buttonsInit();

  // initialize state machine
  //timerControlInit();

  // serial debug
  //Serial.begin(115200);
}

void loop() {
  // read inputs
  //buttonsTick();

  // tick state machine
  //timerControlTick();
}